### Testing task 1:

# Carry out static testing on the code below.
# Comment on any errors that you see below.
```ruby

class CardGame
def checkforAce(card)
    if card.value = 1 # this should be two equals (==)
      #there is no return true here.
    else
      return false
    end
  end
  #\/\/ this should be def not dif
  dif highest_card(card1 card2)# there should be a coma (,) between the arguments.
  if card1.value > card2.value
    return card #this should say card1
  else
    return card2
  end
end
end # this end will end the class it needs to be at the end.
#The self isn't needed here.
def self.cards_total(cards)
  total         #total needs to be total = 0
  for card in cards
    total += card.value   #the return bar below needs to be after an end. This will just return the first value we dont want that.
    return "You have a total of" + total # the total needs to be changed to a string. to_s
  end
end
```
